Sources for the plugin `plugin.appodeal.beta.Smaato`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Smaato" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
